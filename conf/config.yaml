defaults:
  - data: default
  - model: default
  - loss: default
  - training: default
  - curriculum: 01_stage_easy
  - _self_

seed: 42

project_name: naics_gemini
experiment_name: ${curriculum.name}

paths:
  data_dir: ./data
  output_dir: ./outputs
  checkpoint_dir: ./checkpoints
